CREATE TABLE User
(
   FIRST_NAME VARCHAR
  ,LAST_NAME VARCHAR
);
GO;

CREATE VIEW VW_User
AS
SELECT FIRST_NAME, LAST_NAME FROM User
GO;

CREATE PROCEDURE sp_User
AS
SELECT FIRST_NAME, LAST_NAME FROM User
GO;