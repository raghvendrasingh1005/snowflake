CREATE TABLE POC.User
(
   FIRST_NAME VARCHAR
  ,LAST_NAME VARCHAR
);
GO;

CREATE VIEW POC.VW_User
AS
SELECT FIRST_NAME, LAST_NAME FROM User
GO;

CREATE PROCEDURE POC.sp_User
AS
SELECT FIRST_NAME, LAST_NAME FROM User
GO;